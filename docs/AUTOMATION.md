# 🤖 دليل نظام التكامل الآلي لـ WejdanAI

دليل شامل لنظام التكامل المستمر والنشر الآلي الذكي.

---

## 📋 جدول المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المكونات الرئيسية](#المكونات-الرئيسية)
- [GitHub Actions Workflows](#github-actions-workflows)
- [السكربتات الآلية](#السكربتات-الآلية)
- [لوحة التحكم](#لوحة-التحكم)
- [الإعداد والتكوين](#الإعداد-والتكوين)
- [الاستخدام](#الاستخدام)
- [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## 🎯 نظرة عامة

نظام التكامل الآلي لـ WejdanAI هو نظام شامل ومتقدم يوفر:

- ✅ **مراجعة الكود الذكية** باستخدام الذكاء الاصطناعي
- 🚀 **النشر التلقائي** مع اتخاذ قرارات ذكية
- 🧪 **توليد الاختبارات** تلقائياً للكود الجديد
- 📊 **فحص جودة الكود** بشكل مستمر
- 🔒 **فحص أمني** للثغرات المحتملة
- 📈 **مراقبة الأداء** والجودة في الوقت الفعلي

---

## 🏗️ المكونات الرئيسية

### البنية الهيكلية

```
WejdanAI/
├── .github/
│   ├── workflows/
│   │   ├── ai-code-review.yml        # مراجعة الكود الذكية
│   │   ├── auto-deploy.yml           # النشر التلقائي
│   │   ├── code-quality.yml          # فحص الجودة
│   │   └── test-generation.yml       # توليد الاختبارات
│   └── dependabot.yml               # التحديثات التلقائية
├── scripts/
│   ├── auto-deploy.sh              # سكربت النشر
│   ├── quality-check.py            # فحص الجودة
│   └── monitoring-system.js        # نظام المراقبة
├── pages/admin/
│   └── automation.vue              # لوحة التحكم
├── components/automation/          # مكونات الواجهة
└── reports/                        # التقارير المُولدة
```

---

## ⚙️ GitHub Actions Workflows

### 1. مراجعة الكود الذكية (AI Code Review)

**الملف:** `.github/workflows/ai-code-review.yml`

**المهام:**
- فحص الكود تلقائياً عند كل Pull Request
- تشغيل Linting و Type Checking
- توليد مقاييس الكود
- إنشاء تعليقات تلقائية بنتائج المراجعة

**التشغيل:**
```yaml
on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main]
```

**الإعدادات المطلوبة:**
- `GITHUB_TOKEN` (تلقائي)
- `DEEPSEEK_API_KEY` (اختياري للذكاء الاصطناعي)

---

### 2. النشر التلقائي (Auto Deploy)

**الملف:** `.github/workflows/auto-deploy.yml`

**المهام:**
- تحليل التغييرات تلقائياً
- اتخاذ قرار النشر بناءً على التحليل
- بناء التطبيق والاختبارات
- النشر إلى Staging أو Production

**البيئات:**
- **Staging:** النشر التلقائي للفرع `main`
- **Production:** النشر اليدوي عبر `workflow_dispatch`

**كيفية النشر اليدوي:**
```bash
# عبر واجهة GitHub
Actions → Auto Deploy → Run workflow → اختر البيئة
```

---

### 3. فحص جودة الكود (Code Quality)

**الملف:** `.github/workflows/code-quality.yml`

**المهام:**
- فحص ESLint وأنماط الكود
- تحليل تعقيد الكود
- فحص أمني للأنماط الخطيرة
- حساب درجات الجودة
- توليد تقارير شاملة

**الجدول الزمني:**
```yaml
schedule:
  - cron: '0 0 * * 0'  # أسبوعياً كل أحد
```

**درجات الجودة:**
- **قابلية الصيانة** (Maintainability): /10
- **الأمان** (Security): /10
- **الأداء** (Performance): /10
- **الإجمالي** (Overall): متوسط الثلاثة

---

### 4. توليد الاختبارات (Test Generation)

**الملف:** `.github/workflows/test-generation.yml`

**المهام:**
- كشف الملفات المتغيرة
- فحص الاختبارات الموجودة
- توليد قوالب اختبار للملفات الجديدة
- تشغيل الاختبارات
- توليد تقرير التغطية

**التشغيل:**
```yaml
on:
  pull_request:
    paths:
      - 'server/**/*.ts'
      - 'composables/**/*.ts'
```

---

## 📜 السكربتات الآلية

### 1. سكربت النشر التلقائي

**الملف:** `scripts/auto-deploy.sh`

**الاستخدام:**
```bash
# تشغيل النشر التلقائي
./scripts/auto-deploy.sh

# أو عبر npm
pnpm deploy:ai
```

**الوظائف:**
1. فحص البيئة
2. تحليل التغييرات
3. بناء التطبيق
4. تشغيل الاختبارات
5. النشر
6. توليد التقرير

**المتغيرات البيئية:**
- `NODE_ENV` - البيئة (production/staging)
- `VERCEL_TOKEN` - توكن Vercel (اختياري)

---

### 2. فحص جودة الكود

**الملف:** `scripts/quality-check.py`

**الاستخدام:**
```bash
# تشغيل فحص الجودة
python3 scripts/quality-check.py

# أو عبر npm
pnpm quality:check
```

**التحليلات:**
- فحص الأنماط الخطيرة
- تقييم الأمان
- حساب التعقيد
- قابلية الصيانة

**المخرجات:**
- `reports/quality-report.json` - التقرير التفصيلي
- كود الخروج: `0` (نجاح) أو `1` (فشل)

---

### 3. نظام المراقبة الذكي

**الملف:** `scripts/monitoring-system.js`

**الاستخدام:**
```javascript
import { codeMonitor } from '~/scripts/monitoring-system'

// مراقبة توليد الكود
const result = await codeMonitor.monitorGeneration(
  prompt,
  generatedCode,
  responseTime
)

// توليد تقرير
const report = codeMonitor.generateReport()

// حفظ التقرير
await codeMonitor.saveReport()
```

**المقاييس:**
- إجمالي عمليات التوليد
- معدل النجاح
- متوسط وقت الاستجابة
- درجات الجودة
- معدل الخطأ

---

## 🎨 لوحة التحكم

### الوصول

**URL:** `/admin/automation`

### المميزات

1. **بطاقات الإحصائيات**
   - عمليات النشر
   - الكود المُولد
   - جودة الكود
   - الاختبارات

2. **النشرات الأخيرة**
   - سجل النشرات
   - الحالة (نجاح/فشل)
   - الوقت والمدة
   - البيئة

3. **مراقبة الجودة**
   - رسم بياني للجودة
   - الاتجاهات
   - المتوسطات

4. **إجراءات سريعة**
   - تشغيل نشر جديد
   - تشغيل الاختبارات
   - توليد تقرير
   - فحص الأمان

5. **حالة النظام**
   - CI/CD Pipeline
   - النشر التلقائي
   - فحص الجودة
   - الفحص الأمني

6. **التنبيهات الأخيرة**
   - تنبيهات الجودة
   - تنبيهات الأمان
   - تحديثات النظام

---

## 🔧 الإعداد والتكوين

### 1. GitHub Secrets

قم بإضافة المتغيرات التالية في:
`Settings → Secrets and variables → Actions`

**مطلوب:**
- `GITHUB_TOKEN` - يُضاف تلقائياً

**اختياري (للميزات المتقدمة):**
- `DEEPSEEK_API_KEY` - للتحليل الذكي
- `VERCEL_TOKEN` - للنشر على Vercel
- `DISCORD_WEBHOOK` - للإشعارات

---

### 2. إعداد Dependabot

تم تكوين Dependabot لتحديث:
- npm/pnpm dependencies (أسبوعياً)
- GitHub Actions (أسبوعياً)
- Python packages (أسبوعياً)

**التجميعات:**
- Nuxt packages
- Vue packages
- AI engines (OpenAI, Anthropic)
- Development dependencies

---

### 3. الأذونات

تأكد من أن GitHub Actions لديه الأذونات:

```yaml
permissions:
  contents: write
  pull-requests: write
  issues: write
```

---

## 🚀 الاستخدام

### سير العمل الأساسي

1. **تطوير ميزة جديدة:**
   ```bash
   git checkout -b feature/new-feature
   # ... قم بالتطوير
   git add .
   git commit -m "Add new feature"
   git push origin feature/new-feature
   ```

2. **إنشاء Pull Request:**
   - GitHub Actions ستقوم بـ:
     - مراجعة الكود
     - تشغيل الاختبارات
     - فحص الجودة
     - توليد تعليق بالنتائج

3. **دمج في main:**
   - يتم تشغيل workflow النشر التلقائي
   - تحليل التغييرات
   - اتخاذ قرار النشر
   - النشر إلى Staging

4. **النشر إلى Production:**
   - يدوياً عبر GitHub Actions
   - أو تلقائياً بعد اختبارات Staging

---

### الأوامر المتاحة

```bash
# تطوير محلي
pnpm dev

# بناء التطبيق
pnpm build

# فحص الأنواع
pnpm typecheck

# فحص التنسيق
pnpm lint

# نشر تلقائي
pnpm deploy:ai

# فحص الجودة
pnpm quality:check

# مراقبة الأداء
pnpm monitor
```

---

## 📊 التقارير

### أنواع التقارير

1. **تقرير النشر:**
   - `reports/deployment-YYYYMMDD.md`
   - معلومات النشر
   - التغييرات
   - المقاييس

2. **تقرير الجودة:**
   - `reports/quality-report.json`
   - الدرجات التفصيلية
   - المشاكل المكتشفة
   - التوصيات

3. **تقرير المراقبة:**
   - `reports/monitoring-report.json`
   - المقاييس
   - التاريخ
   - التنبيهات

---

## 🔍 الفحوصات والمعايير

### معايير جودة الكود

#### الأمان (Security)
- ❌ تجنب `eval()`, `exec()`
- ❌ تجنب `innerHTML` المباشر
- ✅ استخدام التحقق من المدخلات
- ✅ تشفير البيانات الحساسة

#### الأداء (Performance)
- ❌ تجنب الحلقات المتداخلة العميقة
- ❌ تجنب `querySelector` في الحلقات
- ✅ استخدام `const`/`let` بدلاً من `var`
- ✅ تحسين التكرارات

#### قابلية الصيانة (Maintainability)
- ✅ ملفات أقل من 500 سطر
- ✅ أسطر أقل من 120 حرف
- ✅ تعليقات كافية (5%+)
- ✅ أسماء متغيرات واضحة

---

## ❓ الأسئلة الشائعة

### كيف أعطل workflow معين؟

قم بإضافة `if: false` في الـ workflow:

```yaml
jobs:
  my-job:
    if: false
    runs-on: ubuntu-latest
```

### كيف أغير جدول التشغيل؟

عدّل `schedule.cron` في الـ workflow:

```yaml
schedule:
  - cron: '0 0 * * 1'  # كل اثنين
```

### كيف أضيف فحوصات مخصصة؟

أضف خطوة جديدة في workflow:

```yaml
- name: Custom Check
  run: |
    echo "Running custom check..."
    # your commands here
```

### كيف أتلقى إشعارات؟

أضف webhook في GitHub Secrets:

```bash
DISCORD_WEBHOOK=https://discord.com/api/webhooks/...
```

ثم استخدمه في السكربتات.

---

## 🛠️ الصيانة

### تحديث الاعتماديات

Dependabot سيقوم بذلك تلقائياً، ولكن يمكنك:

```bash
# تحديث npm packages
pnpm update

# تحديث pnpm نفسه
pnpm add -g pnpm

# تحديث GitHub Actions
# يتم تلقائياً عبر Dependabot
```

### تنظيف التقارير القديمة

```bash
# حذف التقارير أقدم من 30 يوم
find reports/ -name "*.md" -mtime +30 -delete
find reports/ -name "*.json" -mtime +30 -delete
```

---

## 📞 الدعم والمساعدة

للمشاكل أو الاقتراحات:

1. **GitHub Issues:** [github.com/MOTEB1989/WejdanAI/issues](https://github.com/MOTEB1989/WejdanAI/issues)
2. **مراجعة التقارير:** تحقق من `reports/`
3. **لوحة التحكم:** `/admin/automation`

---

## 🎯 الخطوات القادمة

- [ ] إضافة تكامل مع Docker
- [ ] نظام A/B Testing
- [ ] مراقبة الأداء في الوقت الفعلي
- [ ] تكامل مع Kubernetes
- [ ] لوحة تحكم متقدمة بالتحليلات

---

## 📝 الترخيص

MIT License - WejdanAI Project

---

**تم التوليد آلياً بواسطة نظام WejdanAI** 🤖
