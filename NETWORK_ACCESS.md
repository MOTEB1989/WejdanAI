# 📱 الاتصال من iPhone/iPad - دليل سريع

---

## ⚠️ المشكلة

`localhost` على iPhone/iPad لا يعمل! لأنه يشير إلى الجهاز نفسه وليس الكمبيوتر.

---

## ✅ الحل - 3 خطوات

### 1️⃣ تأكد من نفس الشبكة WiFi

تأكد أن:
- الكمبيوتر متصل بالـ WiFi
- iPhone/iPad متصل بنفس الـ WiFi

---

### 2️⃣ شغّل Servers على الكمبيوتر

#### Backend (طرفية 1):
```bash
python3 ReasoningOrchestrator
```

يجب أن ترى:
```
INFO: Uvicorn running on http://0.0.0.0:8001
```

#### Frontend (طرفية 2):
```bash
npm run dev
```

سترى شيء مثل:
```
> Local:    http://localhost:3000/
> Network:  http://21.0.0.10:3000/
```

**📝 انسخ رابط Network** (مثال: `21.0.0.10`)

---

### 3️⃣ افتح من iPhone/iPad

في Safari، توجه إلى:
```
http://21.0.0.10:3000/reasoning
```

**✨ استبدل `21.0.0.10` بالـ IP الذي ظهر لك!**

---

## 🔍 كيف تعرف IP الكمبيوتر؟

### على Linux/Mac:
```bash
hostname -I | awk '{print $1}'
```

أو:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

### على Windows:
```cmd
ipconfig
```
ابحث عن **IPv4 Address** تحت WiFi adapter

---

## 📱 الخطوات على iPhone

1. افتح Safari
2. اكتب في شريط العنوان:
   ```
   http://YOUR_IP:3000/reasoning
   ```
   مثال: `http://21.0.0.10:3000/reasoning`

3. اضغط Enter

4. **🎉 يجب أن تفتح الواجهة!**

---

## 🐛 إذا لم يعمل

### المشكلة: "Can't connect to server"

**تحقق من:**

1. **نفس الشبكة؟**
   - Settings → WiFi (تحقق من اسم الشبكة)

2. **الـ Servers شغالة؟**
   ```bash
   # Backend
   curl http://localhost:8001/

   # Frontend
   curl http://localhost:3000/
   ```

3. **Firewall معطل؟**
   ```bash
   # Linux
   sudo ufw allow 3000
   sudo ufw allow 8001

   # Mac
   # System Preferences → Security & Privacy → Firewall
   ```

4. **الـ IP صحيح؟**
   ```bash
   hostname -I
   ```

---

### المشكلة: "Network error when calling API"

**السبب:** Backend غير مرئي من الشبكة

**الحل:**

تأكد أن Backend يعمل على `0.0.0.0`:
```bash
# في ReasoningOrchestrator file
# السطر الأخير يجب أن يكون:
uvicorn.run(app, host="0.0.0.0", port=8001)
```

ثم أعد تشغيله:
```bash
python3 ReasoningOrchestrator
```

---

## ✅ Checklist

قبل الاتصال من iPhone/iPad:

- [ ] الكمبيوتر و iPhone على نفس WiFi
- [ ] Backend يعمل على المنفذ 8001
- [ ] Frontend يعمل على المنفذ 3000
- [ ] عرفت IP الكمبيوتر (مثال: `21.0.0.10`)
- [ ] Firewall يسمح بالمنفذين 3000 و 8001
- [ ] API keys مضافة في `data/reasoning_connections.json`

---

## 🎯 الآن جرب!

### على iPhone:

```
Safari → http://YOUR_IP:3000/reasoning
```

### مثال حقيقي:
```
http://21.0.0.10:3000/reasoning
```

---

## 📲 تثبيت كتطبيق (اختياري)

بعد فتح الرابط:

1. اضغط زر المشاركة
2. "Add to Home Screen"
3. "Add"

الآن يمكنك فتح التطبيق من الشاشة الرئيسية! 🎉

---

## 💡 نصائح

### للأداء الأفضل:
- استخدم WiFi 5GHz إذا متوفر
- تأكد من إشارة WiFi قوية
- أغلق التطبيقات الأخرى على iPhone

### للأمان:
- لا تشارك IP الخاص بك علناً
- استخدم VPN إذا كنت على شبكة عامة
- الـ API يعمل بدون HTTPS (HTTP فقط للتطوير)

---

## 🔄 التحديث التلقائي

الواجهة الآن **تكتشف تلقائياً** الـ hostname:
- على الكمبيوتر: `http://localhost:8001`
- على iPhone: `http://21.0.0.10:8001`

لن تحتاج لتغيير أي كود! ✨

---

## 📚 مزيد من المساعدة

- `SETUP.md` - دليل التشغيل الكامل
- `MOBILE_GUIDE.md` - دليل الموبايل التفصيلي
- `REASONING_ORCHESTRATOR.md` - توثيق النظام

---

**تم إصلاح المشكلة! الآن يعمل على iPhone و iPad 🎉**
